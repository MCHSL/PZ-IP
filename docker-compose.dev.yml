services:
  db:
    image: postgres
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=wklejka
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=haslo123
  backend:
    build: ./Backend
    volumes:
      - ./Backend:/code
    ports:
      - "80:80"
    depends_on:
      - db
